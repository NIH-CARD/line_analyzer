__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/
.ruff_cache/
.pytest_cache/
.DS_Store

# generated: `python -m proteomics_revertant.run` rebuilds these in ~110 s
results/

# fixtures ARE tracked -- tests assert against the shipped answer keys, and the
# .h5 mirrors are small. Regenerate with `make_data --outdir data --hdf5`.

# Working notes for AI coding assistants. Kept out of the repository: it is
# scaffolding for the tooling, not documentation for users of the pipeline,
# and a file named for an assistant reads as noise in a public project.
# The durable engineering content -- the statistical rationale, the output
# reference, the known limitations -- lives in README.md.
CLAUDE.md
